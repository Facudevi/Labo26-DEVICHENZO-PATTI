package sitios_web;
import pagos.Metodo_Pago;
import persona.Cliente;
import tecnologia.Computadora;

import java.util.ArrayList;

public class Web_Computadoras {
    /*
    private Cliente cliente;
    private Metodo_Pago pago;
    private ArrayList<Computadora> listaComputadoras;

    public Web_Computadoras(Cliente cliente, Metodo_Pago pago) {
        this.cliente = cliente;
        this.pago = pago;
        this.listaComputadoras = new ArrayList<>();
    }

    public double calcularPrecioFinal(Computadora pc, Metodo_Pago p){
        double subtotal = pc.calcularSubtotal();
        double totalFinal = p.calcularTotal(subtotal);
        return totalFinal;
    }

    public void procesarCompra(Computadora pc, Metodo_Pago p) {
        if (!pc.validarArmado()) {
            System.out.println("Error: La computadora debe tener al menos una CPU, un entrada y un salida.");
        }
        else {
            listaComputadoras.add(pc);
        }
    }

 */
}